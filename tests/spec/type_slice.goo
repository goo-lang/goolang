// Spec: Types / Slice types — literals, append, len, cap, slicing
package main

import "fmt"

func main() {
	s := []int{1, 2, 3}
	s = append(s, 4)
	t := s[1:3]
	fmt.Println(len(s))
	fmt.Println(len(t))
	fmt.Println(t[0])
}
