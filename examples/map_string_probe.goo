package main

import "fmt"

func main() {
	m := map[string]string{"a": "alpha", "b": "beta"}
	m["c"] = "gamma"
	m["a"] = "ALPHA"
	fmt.Println(m["a"])
	fmt.Println(m["c"])
	fmt.Println(len(m))
	fmt.Println(m["missing"])
	v, ok := m["b"]
	fmt.Println(v)
	fmt.Println(ok)
}
