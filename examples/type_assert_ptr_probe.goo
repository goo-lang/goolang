package main

import "fmt"

type Counter interface {
	Get() int
}

type Box struct {
	N int
}

func (b *Box) Get() int {
	return b.N
}

func main() {
	b := Box{N: 5}
	var c Counter = &b
	p, ok := c.(*Box)
	fmt.Println(ok)
	p.N = 9
	fmt.Println(b.N)
}
