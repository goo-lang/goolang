package main

import "fmt"

func main() {
	m := map[string]int{"a": 1, "b": 2, "c": 3}
	delete(m, "b")
	fmt.Println(len(m))
	fmt.Println(m["b"])
	v, ok := m["b"]
	fmt.Println(v)
	fmt.Println(ok)
	delete(m, "zzz")
	fmt.Println(len(m))
	delete(m, "a")
	delete(m, "c")
	fmt.Println(len(m))
}
