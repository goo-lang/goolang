# ===== SESSION HANDOFF: 2026-07-11 night (correctness arcs 4-7 — const-identifier family CLOSED) =====

## State
`main` @ **`3d26b9d`**, clean, local == origin. **Four PRs merged this session:**
#197 (arc 4: (j) const-ident chan-send + negative-const-decl typing), #198 (arc 5: (h)
const/var-decl gates + shared core extraction), #199 (arc 6: (m) grouped-const misparse
dissolved + typed specs, grammar-only at ZERO tripwire delta), #200 (arc 7: (n) composite
slots + assignments). Gates at merge: golden **436/0**, reject **114/0**, tripwire **31 S/R +
0 R/R exact**, unit 76/1-skip, `make verify-core` ALL GREEN, pre-push CompCert verify passed
on every push. Commits unsigned over SSH (`git -c commit.gpgsign=false`).

## Shipped this session (all merged)
- **The const-identifier fail-open family is CLOSED.** One shared core
  (`check_const_int_expr_fits`, type_checker.c — screen→fold→reconstruct→fit, comptime-param
  screened, bare-ident negated/bare_literal reconstructed from the const's own type) now
  gates: chan-sends (both sinks, arc 4), typed const decls + var-decl initializers (arc 5),
  composite slots (struct fields/slice/array elems/map VALUES via adapt_field_init_value) and
  plain/compound/selector/index/deref assignments (arc 7, shift-assigns excluded as counts).
  Disjointness argument used throughout: is_untyped_int_rooted admits no identifier leaf, so
  literal shapes keep their per-literal adapter semantics bit-for-bit.
- **Arc 4 rider:** negative untyped const decls were mis-typed uint64 (`const k = -5` printed
  huge) — fixed in both mirrors with the negated-shape conjunction.
- **Arc 6 (grammar):** grouped consts silently SHIFTED values across names (`const ( x = 1;
  y int16 = 300; z int8 = 100 )` → 1,1,300). Dissolved structurally: terminated-specs +
  optional-final-spec split (const_spec_t/const_spec_last) confines the bare reduce to ;/)
  lookahead AND admits typed specs at zero conflict delta. Same-line no-`;` juxtaposition now
  a syntax error (Go-aligned). Exec fixture expected output generated from `go run` (skill
  rule 6). v1 wall documented at const_spec_new: bare spec inherits VALUE not TYPE → (q).
- **Process:** fresh-context whole-branch reviews on every arc; arc 4's found a real
  regression (comptime-param placeholder folds — screened), arcs 5-7's came back clean and
  their scope probes produced the backlog below. TDD failing-first on every fixture.

## Next steps (priority order)
1. **(r) MULTI-ASSIGN SEGFAULT** — `x, y = 5, 6` prints correct values then SIGSEGV exit 139
   (arc-7 review find; pre-existing, own AST path — also bypasses all const gates). Only
   crash-class item; recommended next arc.
2. **(i) map-literal KEYS** — even literal keys ungated (`map[int8]int{300:1}` accepts); key
   lookup semantics looked independently off in the probe (printed 0 after store).
3. **(o)** const-ident FALSE-REJECTS at call args + returns (safe direction, shared core
   fixes); **(q)** bare-after-typed group spec skips gate (needs type-node clone or checker-
   side inheritance); **(p)** pkg.K selector consts fall open (folder can't resolve selectors).
4. **(k)** array/slice switch-tag verifier leaks; **(l)** boxing fmt-leg position/comment;
   **(f)/(g)** float-folder family (both want the untyped-float folder built first).
5. Diagnostics family (parked, UX-wide): one-statement-late positions; folded-value-not-
   source message ("constant -1 overflows int64" for MaxUint64); P2.8 poison-cascade leak on
   index-of-rejected-decl (reject-runner hygiene check enforces fixtures avoid it).
6. **Phase 6 M2 decision** still pending — recommendation on record: B2 (comptime-specialized
   numeric kernels) before B1 (NNG transport). Stale PRs #144/#142 still open pre-transfer relics.

## Context to remember
Memories current: `goolang-correctness-followups` (arcs 2-7 MERGED, backlog (r)/(i)/(o)/(p)/
(q)/(k)/(l)/floats), MEMORY.md index refreshed. Scope walls held deliberately: plain-var
narrowing laxness (`var v int64 = 300` into int8 contexts), non-assign arithmetic (`x + a`),
multi-assign path (crashes anyway — see (r)). The v1-roadmap memory still describes pre-M1
Phase 6 state; trust `goolang-parallel-lanes-comptime` + this file over it.
