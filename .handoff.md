# Handoff: 2026-09-03 21:00 AEST

## Goal
Safety first, then performance. Three tracks ran today: phase 4 Bazel probe slices, the verification-tooling branch resumed, and a deterministic-simulation spike for the runtime scheduler.

## Completed
- [x] Phase 4 task 14: PRs #334, #335, #336, #337 merged into `build/bazel-phase4-probes` (head 9ce25cc5, pushed). 22 of 29 script gates run under Bazel; 181 tests. The 7 that stay out cannot run in a sandbox, recorded in `tests/probes/BUILD`. `GOO_PROBE_NO_SKIP`, `needs_cc`, `env` passthrough, thirteen `probe_sources` filegroups, `find -L` plus a scan floor, `cp -rL` in the install script.
- [x] Verify-tooling (worktree `.claude/worktrees/verify-tooling-next-steps`, branch `worktree-verify-tooling-next-steps`, 46 commits ahead of main, NOT pushed): tasks 8, 14, 15 complete with clean reviews. Task 15's per-pair MISRA baseline caught errno/braces regressions in task 8's hook (fixed f564e5a5, logged in `docs/defects.tsv`).
- [x] Cobra calibration spec: `docs/superpowers/specs/2026-09-03-cobra-calibration.md` (88e4504c), awaiting the user's go before task 12.
- [x] Simulation spec and eight-task plan: `docs/superpowers/specs/2026-09-03-deterministic-simulation-design.md`, `docs/superpowers/plans/2026-09-03-deterministic-simulation.md` (cd7fe091), approved section by section.

## In Progress
- [ ] Verify-tooling: tasks 8, 14, 15, 16 complete (16 at 7bfeb4c6). Task 17 (one SARIF findings database, commits 21b424fe..b53df609) is in fix round 1 after an Opus review measured four Important defects; rulings in the ledger: counts per (tool, rule, file) not per fingerprint, absolute paths outside the tree, a baseline keyed by toolchain signature that is loud when absent, suppressions must name an existing deviation record. Then a scoped re-review, then task 18 (notes ready: task-18-dispatch-notes.md), 19.
- [ ] Sim: Tasks 1, 2 and 3 complete at 146b25d4, all reviewed clean (seam, workers and TLS, the `runtime-seam-probe` gate; warning ratchet 77 → 46, pthread ratchet 40 → 17, parity 229 on that branch). NEXT: Task 4, the simulator's scheduler, clock and trace, on Opus from `task-4-brief.md`, BASE 146b25d4. Its T3 nesting measurement is the spike's first real question; approach B is the named fallback.

## Blockers
- None. Both fix rounds may finish after this session; their commits land in the worktrees and each ledger records them.

## Open Questions
- The user's go on the Cobra spec.
- `~/.local/bin/{cobra,spin,herd7}` are the user's own installs (14:38, 14:52); the spin self-test now scrubs PATH.

## Key Files Modified
- Ledgers (recovery maps): `.superpowers/sdd/2026-08-25-bazel-phase4-probes/progress.md` (main checkout), `<verify-tooling worktree>/.superpowers/sdd/2026-09-02-verification-tooling-next-steps/progress.md`, `<sim worktree>/.superpowers/sdd/2026-09-03-deterministic-simulation/progress.md`.

## Next Steps
1. Read the two in-flight results (task 17 fix round 1; sim Task 3) from the ledgers and `git log` in each worktree; re-review; close.
2. Verify-tooling: tasks 17-19, then 12-13 after the Cobra go, 22, final Opus review, push, PR to main FIRST (merge order decided: before phase 4).
3. Sim: Tasks 2-8 per the plan; Task 4's nesting measurement decides approach A or the B fallback.
4. Later: `zig cc` toolchain spike; a `tigerstyle.cobra` rule set; a runtime fault-model document.

## Context to Remember
Never commit in a worktree while an implementer runs there. A bare `bazel test //...` in the main checkout fails on stale BUILD files under `.claude/worktrees/`; use `-- -//.claude/...`. Subagents park on Monitor waits; nudge them to read logs directly.
