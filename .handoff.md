# Session handoff — the ARC leg stopped guessing and started measuring

Updated: 2026-07-28 (evening). **THREE PRs merged** (#255 escape-core
extraction + the map-key fix, #256 shim escape summaries, #257 immortal string
literals). Five planned increments landed. One remains, scoped and decided.

## Where we are

`main` @ `e0c66a9`, working tree clean, PR queue EMPTY, `verify-core` ALL GREEN
post-merge.

Counts now: goldens **493/493** at BOTH -O0 and -O2, golden-reject **156/156**,
tripwire **31 S/R / 0 R/R** exact, param_escape **23 rows**, block_escape **31
rows**, local_escape **16 rows**, obj_header **34 checks**, arena-valgrind
**17/17** clean. New probe: `string-literal-header-probe`, in verify-core.

## THE HEADLINE — the plan's next increment would have reclaimed NOTHING

The session opened by planning the first ARC release consumer. The first task
was a measurement, and it invalidated the four tasks behind it. That was the
whole value of doing it first.

`scripts/daemon_alloc_attribution.sh` runs massif (LIVE bytes) and dhat (block
counts). Full write-up:
`docs/superpowers/specs/2026-07-28-daemon-alloc-attribution-findings.md`.

**Attribution, 1,377 B live in 44 blocks per request:**

| Share | Site |
|---|---|
| 20.3% | `goo_map_set_sv` |
| 19.0% | `goo_strings_trim_space` |
| 17.9% | `goo_strings_split` (two sites) |
| 12.3% | the error path (`goo_error_wrap` x2) |
| 10.5% | `goo_slice_append` |

**No single site holds more than a fifth.** There is no one fix.

Two numbers worth keeping: **44 live blocks per request, average payload ~15
bytes.** The allocation COUNT, not the byte total, is what a uniform ARC would
pay for — at the measured 10.17 ns per atomic retain/release pair, that is at
least 44 pairs per request.

**The ledger's error-path row under-counted by 60%.** It said 106 B/request;
measured is 170 B in 4 blocks.

## Method notes that earned their keep

- **VERIFY THE INSTRUMENT, and it caught two of my own tools.**
  1. A function extractor matched FORWARD DECLARATIONS, so `expr_taint` came
     out as 22 lines instead of 135. Three equally-wrong extractions diffed
     clean against each other and reported "0 differences" for functions that
     genuinely differ. Exactly the trap `.handoff.md` already recorded as "a
     comparison of two EMPTY files reported IDENTICAL".
  2. Running `local_escape_analyze` directly on `bench/daemon/daemon.goo`
     printed a confident full table — but the imports had not resolved, so every
     verdict was conservative for an unrelated reason. **The tell was `i`, a
     plain int loop counter, reading as escaping.** Import-free reproductions
     were the only way to get one cause per verdict. They are kept at
     `docs/adr/0002-measurements/local_escape_shapes.goo` with the harness
     beside them.
- **Two instruments, because they disagree.** dhat's total-allocated overstates
  `goo_slice_append` by 2.1x — `parts` doubles 1-2-4-8 and realloc frees three
  of the four buffers. massif's peak shows only the 144 B that survives.
- **Predicting cost by multiplication overstated it 18x.** The immortal header
  is nominally 16 B x 54 literals = 864 B. Measured: +48 B. `unnamed_addr` lets
  the linker merge identical constants and .rodata was already padded.
- **Piped exit codes lied again.** A mutation run printed `exit=0` while the
  binary had dumped core; the "Aborted (core dumped)" line was the real
  evidence. Read the output, never the pipeline's status.

## What landed

### PR #255 — one escape engine, and the hole it made visible

`param_escape`, `block_escape` and `local_escape` ran three hand-maintained
copies of the same walk. The divergence was MEASURED before anything moved:
`walk_stmt` is ~200 lines in each, and `param` differed by ONE line (the return
signal) while `local` differed only by which bind function it called, at five
sites. `expr_taint` differed only by block's three source arms. `handle_go_call`
and the taint-set helpers were byte-identical in all three.

The whole difference is **six hooks**. `src/types/escape_core.{c,h}` is the
engine. 3,434 lines -> 2,497 including the new header.

**Proven live, not assumed:** deleting the `AST_POSTFIX_EXPR` arm from the
SHARED core reproduces the original defect (local-escape row 4 FAILS). That
same mutation left `param` and `block` green — **so neither of those 51 rows
covers `i++`.** That gap is real and still open.

Then, in one nine-line arm rather than three mirrored edits:

**A MAP KEY IS AN UNTRACKED REFERENCE.** `assign_to_lvalue` marked only the
RIGHT-hand taint, so the index of a non-identifier lvalue was never tainted.
Measured: in `m[f] = 1; return m` the map ESCAPED and `f` did NOT, yet
`goo_map_set_sv` keeps the key pointer verbatim and never frees it. A release
consumer would free a buffer a live returned map still holds. Sink #2b marks
the INDEX only, never the base. Rows 15 and 16, where 16 is the slice contrast
that was already sound because `append` is an ordinary call.

**The extraction paid for itself within one commit.**

### PR #256 — summarise the C shims, and unblock 30% of the daemon

T0's dominant finding: a C shim has no Goo body, so `param_escape_lookup`
misses and `call_taint` takes its pure-conservative branch. **Every local passed
to any stdlib call was marked escaping for that reason alone.**

The mechanism already existed with the right meaning — `goo_callee_is_non_retaining`'s
`whitelisted` branch skips the argument mark AND empties the result taint. It
was hardcoded to six `fmt` selectors. This is one new column on the shim
signature table, and those six `fmt` entries moved into it unchanged.

**NO separate `returns_borrowed` flag.** The plan asked for two facts and one
bit carries both, because the consumer already treats them as one.
`errors.Unwrap` proves the shape is right: it retains nothing but returns
`e->cause`, a pointer INTO its argument. A retention-only flag would have
marked it safe.

Every `1` is justified against the runtime C body beside its row. Unaudited
stays `0`: `os.*`, `far.*`, `fmt.Errorf` (%w retains), `fmt.Fprint*` (the writer
is an `io.Writer`, so a USER `Write` can store what it is handed).

Answering by package name needed a guard: a local named `strings` would
otherwise collect the package's whitelist. `selector_base_is_local` uses the
walk's own `LocalEnv`. That closes the same pre-existing hole for `fmt`.

**Measured on the real daemon:** `fields` and `err` flipped to non-escaping —
**30.2% of retained bytes**. `f` correctly stays escaping because the map holds
it as a key.

**Still inert for arenas, live only for locals.** No shim takes a user pointer,
so no arena value can reach a whitelisted call.

### PR #257 — a string literal is a real ARC object

A literal was the **FOURTH headerless pointer kind, and the only one nothing
excluded**. `codegen_const_string_value` emitted a bare byte array, so a release
would compute `global - 16` and hand .rodata to `free()`. NULL and
`goo_zerobase` are checked; an arena pointer has a static proof; a literal had
nothing. `last := ""` at `bench/daemon/daemon.goo:47` is that shape.

The global now carries a header with `GOO_RC_IMMORTAL` (`UINT64_MAX`), and
retain/release/free are no-ops on it. **UINT64_MAX not 0**, because 0 already
means "not a counted object" and reusing it would make `goo_release`'s underflow
panic unreachable.

Two gates, because neither half proves the other: `obj_header_test` row 15
tests the runtime (fails with "free(): invalid size" + core dump when the guard
goes), and `string-literal-header-probe` tests codegen's IR (fails when the
sentinel is wrong). The probe also runs `opt -passes=verify`.

## NEXT — T4, RETARGETED by measurement. Target `fields`, NOT `err`.

**Emit `goo_release` for owned, non-escaping, FUNCTION-SCOPE locals at function
exit.** Shallow (outer object only), as chosen before.

**The previous scope said ERROR locals first, and that reclaims ZERO.** Full
measurement: `docs/adr/0002-measurements/t4_condition2_findings.md`.

`err` is LOOP-BOUND — `n, err := strconv.Atoi(f)` sits inside `handle`'s `for`
loop. `local_escape` gives ONE boolean per local NAME for the whole function and
the engine has no kill rule, so a function-exit release frees whatever the local
holds at that moment: one of N, leaking N-1. On the daemon's actual request
(`"1, 2, 3, four, 5, six, 7"`) the last field parses, so `err` is **nil** at
exit and `goo_release(NULL)` is a checked no-op. Nothing is freed.

The earlier choice picked `err` because it is "a plain pointer, one owner, no
container". That was right about the code shape and wrong about the bytes,
because SCOPE was not in the comparison.

| Candidate | Scope | Function-exit release reclaims | Not reclaimed (shallow) |
|---|---|---|---|
| `err` | inside the loop, rebound 7x | **0 B** — holds nil at exit | the 90 B of message copies |
| `fields` | function scope, bound once | **128 B/req, 9.3%** | its 6 element strings, 118 B, 8.6% |

Releasing a loop-bound local IS the kill rule, and ADR 0004 records that the
kill rule and the CFG are ONE item of work. So `err` is not a small follow-up to
`fields`. It is the CFG.

The full 30.2% still needs TRANSITIVE release, a separate and larger design.

**Three conditions must all hold before emitting a release, plus a fourth that
the measurement added:**
1. `local_escape` says the value does not outlive the function.
2. The local OWNS the value (not a borrowed view).
3. The binding site is NOT arena-routed.
4. **The local is bound ONCE, at function scope, not inside a loop.**

**Condition 2 has an answer now, and it is NOT the one ADR 0004 proposed.**
Origin-emptiness is constant-false: `local_escape`'s slots are local indices and
a local keeps its own bit for life, so no local's taint is ever empty. Use a
BINDING-SITE rule instead — `new(T)`/`&T{}` is owned, a Goo call is owned iff the
callee's `return_escapes` is false, a shim call is owned iff `non_retaining` is
1, and a slice/index/selector or another local is NOT owned. Conservative on any
miss. The rule and its measurement are in the findings document.

**Condition 4 needs a scope fact that `LocalEscapeResult` does not carry** (it is
a name plus a boolean). Resolved by giving the new module its own small walk —
see below.

### T4 increment A — LANDED: the decision, without the emission

`src/types/release_decision.{c,h}` answers all four conditions. It lives in
`src/types/` and not in codegen for the same reason its three siblings do: pure,
no LLVM, table-testable. Codegen will only READ a verdict, the way it already
reads `block_escape`'s.

**The soundness direction is INVERTED from the escape passes.** There
`escapes = true` is safe and the default arm marks everything. Here
`release = false` is safe, so every unrecognised path refuses and says why. A
wrong refusal costs reclamation; a wrong approval frees live memory.

`ReleaseVerdict` carries the CAUSE, not just a boolean, so a row can assert why a
local was refused. Gated by `release-decision-test` (17 rows, 55 assertions) in
`make verify-core`.

**A fifth condition the plan did not have: RE-ASSIGNMENT.** `a := new(int)`
followed by `a = t.borrowedField` leaves `a` holding something it does not own,
yet the DECLARATION site was a clean allocation. Condition 2 reads the binding
site, so only counting bindings catches it. `RELEASE_NO_REBOUND`.

**Increment B, still to do:** emit the release. Wire the plan into codegen beside
`block_escape` (`src/codegen/codegen.c:355`), emit `goo_release` at each function
exit AFTER `codegen_emit_deferred_calls` (~8 call sites), and gate it with an
arena-style valgrind probe on the daemon plus an RSS measurement that shows the
9.3% actually arrives. Nothing frees memory yet.

**Condition 3 is a soundness rule, not a tidy-up.** Inside `arena { }` a local
can hold an arena pointer, which has no header. `local_escape`'s boundary is the
FUNCTION, so such a local reads as non-escaping and a release would free an
interior pointer. The coexistence spec covers PARAMETERS only. For the first
cut, skip every local declared inside an `arena { }` block.

**And a fourth question T4 must answer before it emits anything: do releases run
BEFORE or AFTER a function's deferred calls?** If before, a deferred call reading
the local dangles. This is why `local_escape` still carries
`defer_is_like_go = true` (see below).

## Ledger — open, each with a reason

| Item | Why not yet |
|---|---|
| ~~`param_escape` and `block_escape` have no `i++` row~~ | **CLOSED.** param row 24 and block row 32. Both verified to fail with the arm mutated and pass with it restored, and `--reach` went 0/0/6 -> 2/4/6. Self-test control 2 now guards them |
| **11 of 16 engine arms had NO soundness coverage in ANY suite** | MEASURED, not suspected: `docs/adr/0002-measurements/escape_arm_coverage.md`. Five of them closed in `local` (rows 17-22), because `local` is the pass T4 consumes. param and block still need their own. Under-marking is the only bug class that can dangle a pointer, so this is T4's real exposure |
| ~~`escape_walk_stmt`'s 20 statement arms are unmeasured~~ | **CLOSED.** Measured in both directions plus reach. `AST_MULTI_ASSIGN`, `switch`, `type switch` and `select` were reached by NONE of the 78 rows — all sinks, and the last three RECURSE into a nested body, so a skipped arm leaves a whole body unwalked. local rows 23-27 close five, taking local from 7 to 12 of 18 testable arms |
| Statement arms still uncovered in `local` | `AST_IF_LET_STMT`, `AST_CONST_DECL`, `AST_UNSAFE_STMT`, `AST_ASSERT_STMT`, `AST_ASSUME_STMT`, `AST_DEFER_STMT`. The first five have 0 reach in every suite. `AST_DEFER_STMT` is reached only in block, and it is entangled with `defer_is_like_go` and T4's release-versus-defer ordering, so a row should follow that decision rather than lead it |
| Statement soundness in param and block | The five rows went into `local`, the pass T4 consumes. `AST_MULTI_ASSIGN`, `switch`, `type switch`, `select` are still GAP in both other suites |
| `release_decision`'s walk does not read `switch`, `type switch`, `select` or `if let` | So a function containing ANY of them is refused ENTIRELY (`RELEASE_NO_UNKNOWN`), which is a real precision cliff — those statements are everywhere in real code. Deliberate and SOUND for the first cut: an unread statement might assign to a local, and a missed assignment would let a rebound local be released. Pinned by row 17 so it cannot drift into looking intentional-forever. Lifting it needs only recursion into those bodies, plus its own rows. The daemon's `handle` has none of them, so T4's target is unaffected |
| The teeth harness caught a defect in ITSELF before it caught anything else | `scripts/release_decision_teeth.sh` first used `git checkout --` to undo each mutation. The file was UNTRACKED, so the restore silently did nothing, `2>/dev/null` hid the error, and the deletions became CUMULATIVE — every condition reported CAUGHT with row counts climbing 2/9/10/12/12/12. It now uses a verified copy-based backup and asserts the file matches before each mutation. The honest counts are 2/7/1/2/1/1, and TWO conditions were genuinely unguarded until rows 16 and 17 were added |
| Five arms are never reached by any fixture | `AST_SLICE_INDEX_EXPR`, `AST_ARRAY_LITERAL`, `AST_KEYED_ELEMENT`, `AST_SLICE_CONVERSION`, `AST_TYPE_ASSERT`, still 0 hits. Whether the cause is "no fixture" or "the front end never builds this node" is UNDETERMINED. A fixture attempt is the only way to tell, and guessing either way would be a false finding |
| `AST_POSTFIX_EXPR` has no soundness coverage anywhere | NOT a hole for the common shape, and this is the reason: in Go `i++` is a statement, never an expression, and the expression-statement arm computes the taint and frees it WITHOUT marking. Under-marking the arm is unobservable for `i++`. Only an operand carrying taint or side effects (`arr[f(p)]++`) makes it observable |
| A map READ marks its key escaping | Over-conservative and pinned deliberately as local row 22, the same treatment block row 31 gets. `goo_map_get` stores nothing, so only the base needs marking. MEASURED reason not to fix it: the daemon's string local escapes through a map WRITE, so tightening the read rule reclaims ~0% and would be a soundness-relevant change to the shared engine |
| `local_escape` defer treatment is over-conservative | It inherits `defer_is_like_go = true` from block_escape, whose comment about "AFTER this arena block has closed" is FALSE for a function boundary. Sound but imprecise. Tightening depends on T4's release-vs-defer ordering, which is undecided. The hook now NAMES that dependency instead of hiding it in a copied comment |
| Map keys/values still untracked at runtime | The analysis is now sound (sink #2b marks the key), but `goo_map_set_sv` still keeps the pointer and never frees. 23.2% of the daemon and unreachable by any codegen-side release — that is the map-held-references spec's own argument |
| Slices of pointers | `goo_slice_append` copies raw bytes, so nothing counts an appended pointer. ADR 0002 lists it open. Blocks transitive release |
| `os.*` / `far.*` shim retention unaudited | All `0`, which is safe. Auditing them widens the whitelist further |
| Named results are NOT in `LocalEnv` | Unchanged. Still the only sink covering `p = q; return` with a named result. Pinned by param_escape row 20 |
| Method summaries resolve by BARE name | Unchanged. `registry_find` returns the first match |
| Cycles | ADR 0002 names this as the real cost with no cheap fix |
| interface == interface fails codegen | Pre-existing, untouched |
| `types.c:603-607` frees interface methods as an ARRAY | Latent, pre-existing |

## Still true, and still unaddressed

- **There is no CI.** `gh pr view --json statusCheckRollup` returns `[]`. All
  three merges today rode on this one machine's local hooks, which
  `git push --no-verify` bypasses. Standing open decision, because it costs
  money.
- Four conformance rows remain absent: `type_complex`,
  `expr_method_expression`, `generic_constraint_union`, `generic_type`.
- `ownership_checker.c` still compiles into `bin/goo` with ZERO call sites.

## The direction, stated plainly

Nothing this session freed a single byte. Two of the three PRs made the
analysis MORE conservative, not less. What they bought is that the next
increment can be trusted: the engine is one copy, the map-key hole is closed,
a literal can no longer be handed to `free()`, and the target is chosen from a
measured table instead of an estimate.

The measurement was worth more than the four tasks it invalidated.
