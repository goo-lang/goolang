# Session handoff — the compiler reclaims memory now, and the daemon lost 115.8 MB

Updated: 2026-07-29 (evening). **TWELVE PRs merged** (#258 arm coverage, #259 the
T4 ownership spike, #260 the release decision, #261 the first emission, #262 slice
release, #263 the self-append idiom, #264 string release, #265 condition 5,
#266 the switch precision cliff, #267 the entry-block zero store, #268 the select comm fix, #269 the map destructor). PR queue EMPTY.

## Where we are

`main` @ `b4ae8c5`, working tree clean, `verify-core` ALL GREEN.

Counts: goldens **493/493** at BOTH -O0 and -O2, golden-reject **156/156**,
param_escape **25 rows**, block_escape **33 rows**, local_escape **28 rows**,
release_decision **34 rows / 112 assertions**, obj_header **41 checks**,
arena-valgrind **17/17**, arena-rss **4/4**, arc-release-probe **19 checks**.

New gates this session: `release-decision-test`, `arc-release-probe`.

## THE HEADLINE — T4 shipped. The compiler frees memory.

ADR 0002 decided ARC in the abstract. This session made it real.

| `bench/daemon/daemon.goo`, 400,000 requests | peak RSS |
|---|---|
| `GOO_ARC_RELEASE=0` (the control) | 750,996 KB |
| default | **632,464 KB** |

**115.8 MB reclaimed, 15.8% of peak.** Output identical either way. And on the
no-arena control of `arena_return_reclaim_probe`, 200,000 iterations of a 2 KB
object: **407,636 KB → 1,956 KB**, a 208x reduction.

Before this session nothing in the compiler had ever freed a heap object outside
an `arena { }` block.

## How it got there, in the order it had to happen

| Increment | What | Daemon |
|---|---|---|
| #260 | The DECISION: `src/types/release_decision.{c,h}`, four conditions, no emission | — |
| #261 | The EMISSION at function exit, bare-pointer slots only | **0%** |
| #262 | Slice release: the buffer is FIELD 0 of a fat value | 54.8 MB |
| #263 | The self-append idiom `L = append(L, x)` | **115.8 MB** |
| #264 | String release, plus concatenation as an owned binding | ~0%, and 490 KB on its own probe |
| #265 | Condition 5: refuse a local declared in a conditional block | closed a LIVE use-of-undef bug |
| #266 | The walk reads `switch`, `type switch`, `select`, `if let` | ~0%, and 240 KB on its own probe |
| #267 | Entry-block zero store; condition 5 RETIRED | ~0%, and the cond probe went 24 -> 0 bytes |
| #268 | `escape_core`: a select case's comm is an EXPRESSION | ~0%, and row 32's tripwire fired |
| #269 | `goo_release_with` + `goo_map_dtor`: a map frees its entry chain | ~0% (PR A of two), and its probe went 800,000 -> 0 |

## THE PATTERN THAT DEFINED THIS SESSION — measurement invalidated the plan FIVE times

Every one of these was caught by measuring first, and each would have shipped a
correct-looking change worth nothing.

1. **The postfix ledger item was real but understated.** 11 of 16 testable engine
   arms had NO soundness coverage in ANY suite, not just the one arm the ledger
   named.
2. **`AST_MULTI_ASSIGN`, `switch`, `type switch` and `select` were reached by NONE
   of 78 rows** — all sinks, and the last three recurse into a nested body.
3. **T4's chosen target `err` reclaims ZERO.** Loop-bound, and nil at exit on the
   daemon's input. The plan projected 5.8%.
4. **The retargeted `fields` also reclaimed zero at first.** A slice is a fat
   value, so the pointer-only emission skipped it.
5. **`append` alone was worth zero.** `RELEASE_NO_ESCAPES` → `RELEASE_NO_REBOUND`
   → `RELEASE_OK` needed BOTH the append summary and the self-append rule.

## FOUR INSTRUMENT DEFECTS, and every one produced plausible output

Two of them reported all-green. This is the session's most transferable lesson:
**check the input, not the output.**

1. **`git checkout --` silently no-oped on an UNTRACKED file**, and `2>/dev/null`
   hid the error, so mutations became CUMULATIVE. All six conditions reported
   CAUGHT with row counts climbing 2/9/10/12/12/12 — monotonic BECAUSE each run
   carried the previous deletions. Honest counts: 2/7/1/2/1/1, and two conditions
   were genuinely unguarded.
2. **An extractor returned EMPTY for `case AST_X: {`**, and the emptiness test then
   called 15 of 20 statement arms no-ops, `AST_RETURN_STMT` among them. Harmless
   for years under one test's polarity; catastrophic the moment a test asked "is
   this empty?".
3. **A self-test control asserted something false about the code.** `block_escape`
   runs two passes, so `walk_stmt`'s `AST_ARENA_BLOCK` arm only ever sees a NESTED
   block. The control was wrong, not the harness.
4. **`escape_arm_coverage.sh` DESTROYED UNCOMMITTED WORK.** Its clean-engine guard
   called `die`, which fired the EXIT trap, which ran `git checkout --` and deleted
   the very edit the guard objected to. Fixed: `restore_engine` refuses to act
   unless an injection landed.

Also: `tee | head` truncated a saved matrix to 5 of 17 rows via SIGPIPE, and zsh
does not word-split an unquoted `$OBJS`.

**A FIFTH, found in #264: valgrind is BLIND to half of the substring hazard.**
Measured by making `binding_is_owned` accept `AST_SLICE_INDEX_EXPR`:

- `low=1` -- `interior - 16` lands in the REAL header, reads refcount 0, and
  `goo_release` panics. Exit 2, and the rc check catches it.
- `low=32` -- it lands in the PAYLOAD, reads a large non-zero count, so the
  underflow panic never fires and the count never reaches 0. The atomic
  `fetch_sub` then WRITES a decrement into the middle of the string and returns.
  No `free()`, NO VALGRIND ERROR, exit 0 -- SILENT CORRUPTION of eight bytes.

So the runtime's refcount-0 panic is an ACCIDENTAL defence whose reach depends on
the offset, and it must never be relied on. `examples/arc_release_substring_probe
.goo` therefore prints its string, and the gate diffs that output against the
`GOO_ARC_RELEASE=0` control. A valgrind-only check had no teeth against half of
its own subject.

**A LIVE BUG, and the plan that found it was looking somewhere else (#265).**
While planning the switch cliff, reading `codegen_alloc_local_promoted` showed
that an ordinary local gets an ENTRY-BLOCK alloca while its initialising store
stays at the declaration site. So a local declared in a conditional block has a
slot on every path and a VALUE only on the taken one, and an LLVM alloca is undef.
Measured on `if c { a := new(int) }` with c false:

    Use of uninitialised value of size 8
       at goo_release (runtime.c:203)      <- the immortal-count read
     Uninitialised value was created by a stack allocation at f

and again at runtime.c:215, the `__atomic_fetch_sub` -- a WRITE through the
garbage pointer to an arbitrary address. **The program still exited 0.** Only
valgrind saw it. Reachable through a plain `if` since #261, unnoticed for four
increments, and found by reading code rather than by any test.

**AND THE PROBE FOR IT WAS VACUOUS ON THE FIRST DRAFT.** `sink = sink + *alive`
tripped the `AST_UNARY_EXPR` arm, marked `alive` escaping, and released NOTHING --
the probe passed while measuring nothing. Caught by checking the `[arc]` debug
line, not by review. `examples/arc_release_probe.goo` already records that arm as
the reason it increments a counter; the lesson did not transfer until it was
measured again.

**#267, AND THE COMMENT WAS WRONG BEFORE THE MEASUREMENT WAS.** The entry-block
zero store had to go immediately after the alloca, NOT before the entry block's
terminator the way `defer_entry_store_zero` does — when it runs, the entry block
has no terminator yet, so "before the terminator" degrades to "at the end", after
a function-scope local's own initialising store. The first draft of the comment
asserted this would "silently become a no-op and leak". Building that variant
showed the real symptom: an immediate nil-deref panic, exit 2, with a 24-byte
leak that is the panic and not a near miss. The claim was right and the mechanism
in the comment was not, and only the build separated them.

**#268, AND AN OVER-CONSERVATIVE DEFAULT HIDES HOW LITTLE THE SUITES TEST.**
Fixing the select comm arm made SEVEN matrix cells move from GAP to COVERED
without a single row being written for them. The cause: while `escape_mark_all`
marked every site regardless, a mutation to an expression arm could not change
any verdict, so those cells read GAP. `escape_mark_all` was masking arm coverage
as well as costing precision. Any future `default:` that marks everything should
be read the same way — it makes the matrix under-report.

**#269, AND THE SAME PARTIAL READ TWICE IN ONE AFTERNOON.** valgrind reports
`definitely lost` and `indirectly lost` separately, and a container that holds
leaked children lands in a `(X direct, Y indirect)` record that a naive grep does
not match. The element-scan spike hit this first: its table omitted the two
LARGEST records, including the map at 40.8%. Three hours later the map probe hit
it again — OFF and ON both read 400,000 `definitely lost`, which looks like "the
release does nothing", when OFF was 1,200,000 total and ON was 800,000. **When
reading a valgrind leak, read definite AND indirect, and check the parsed total
against the summary line.** Both times the tell was a number that looked too
tidy.

## The design, for whoever picks this up

**Four conditions, all of which must hold** (`include/release_decision.h`):

1. `local_escape` says the value does not outlive the function.
2. The local OWNS the value, decided at the BINDING SITE.
3. Not declared inside `arena { }` (an arena pointer has no header).
4. Bound ONCE at function scope — not in a loop, and not re-assigned.

**The soundness direction is INVERTED from the escape passes.** There
`escapes = true` is safe. Here `release = false` is safe, so every unrecognised
path refuses and records WHY. `ReleaseVerdict` carries the cause, and every row
asserts the cause rather than a bare boolean.

**Condition 2 is a binding-site table, NOT ADR 0004's origin-emptiness.** That
proposal is constant-false: `local_escape`'s slots ARE local indices and a local
keeps its own bit for life, so no local's taint is ever empty. Measured in #259.
The working rule: `new(T)`/`&T{}` and composite literals are owned, a Goo call is
owned iff its callee's `return_escapes` is false, a shim call iff `non_retaining`
is 1, and a slice/index/selector or bare identifier is NOT owned.

**`ArcReleaseSite.field` says WHICH part of a slot is the object.** `-1` is the
slot's contents (a bare pointer, and a map, whose LLVM form is already `i8*`).
`0` is a fat value's buffer, and only `TYPE_SLICE` uses it.
**`TYPE_INTERFACE` is `{ vtable*, data* }` — field 0 there is the VTABLE, so there
is no general "release field 0" rule and never can be.**

**Ordering is STRUCTURAL.** `codegen_emit_deferred_calls` is a wrapper that runs
defers then releases, because a Go defer runs before the return and can read a
local through a closure. The 8 exit paths cannot drift.

**The release list is captured at DECLARATION time**, in `vscope_add`. Reading the
value table at exit emitted NOTHING — the function body is a block statement, so
`vscope_exit` truncates its locals first.

**`GOO_ARC_RELEASE=0` is the kill switch.** It leaves the plan NULL and emits
nothing. Every probe and `arena_rss_probe.sh` uses it as the control.

## NEXT, in the order I would take it

1. **PR B: THE SELF-STORE RULE.** The one item with measured bytes behind it.
   `assign_to_lvalue` (`src/types/escape_core.c:202`) marks `rhs_taint` for a
   non-identifier lvalue, and `counts[f] + 1` carries the map's OWN taint, so
   `counts[f] = counts[f] + 1` marks the map escaping because of its own
   contents. MEASURED by narrowing: a plain write, a param key, a write in a
   loop, a write with an import, and a function returning a string ALL release.
   Only the compound update refuses.
   The rule: when the lvalue is `B[k]`, subtract B's own self-bit before marking.
   Same shape as release_decision's self-append. `outer[k] = inner[j]` still
   marks `inner`.
   Worth **822,000 of 2,209,982** per 2,000 daemon requests — the map's entry
   chain, which #269 can already free and simply never gets asked to.
   CAUTIONS: `assign_to_lvalue` is shared by all three passes, so re-run both arm
   matrices (#268's tables are the baseline). Under-marking is the dangerous
   direction, and `mark_lvalue_subscripts` records that an unmarked map key was
   exactly the defect class that dangles a pointer — local-escape row 15 must
   stay green.
2. **THE `AST_UNARY_EXPR` ARM**, which returns the operand's taint for every
   unary operator, so `return *p` marks `p` escaping though the deref copies the
   pointee out. It has cost THREE probe drafts. NOT an arm change: no escape
   pass reads a type at all, so it needs `ASTNode.node_type` and is the first
   type dependency in `escape_core`. DECIDED: drop the taint only for a proven
   non-pointer scalar, conservative on NULL.
3. **A `[]T` element scan.** DEMOTED, and the spike is why. Its ceiling is about
   20% of what remains, not the largest item, and "bounded on its own" did not
   survive contact: `append`'s elements genuinely escape, so "release the
   elements with the slice" needs a NEW ownership rule with no precedent to copy.
   Contrast the map, whose rule was already written.
4. **The `strconv.Atoi` error objects, 15.4%.** Third-largest and nobody has
   costed them. They are loop-scoped locals, so condition 4 refuses them — the
   ADR 0004 kill rule / CFG problem, not a container problem.
5. **Transitive release** is the rest, and it is a genuinely larger design. Note
   the shares are MEASURED now (element_scan_spike.md), not estimated: the map
   is 40.8% and not the 20.3% this file used to say.

## Ledger — open, each with a reason

| Item | Why not yet |
|---|---|
| `param_escape`/`block_escape` lack FOUR of the five statement rows | `select` is now COVERED in all three suites (#268 added param row 25 and block row 33). `MULTI_ASSIGN`, `switch`, `type switch` and `if` are still GAP in both |
| Six statement arms uncovered in `local` | `IF_LET`, `CONST_DECL`, `UNSAFE`, `ASSERT`, `ASSUME` have 0 reach in every suite. `DEFER_STMT` is reached only in block, and it is entangled with `defer_is_like_go` and T4's release-versus-defer ordering, so a row should follow that decision rather than lead it |
| Five expression arms never reached | `SLICE_INDEX_EXPR`, `ARRAY_LITERAL`, `KEYED_ELEMENT`, `SLICE_CONVERSION`, `TYPE_ASSERT`. Whether the cause is "no fixture" or "the front end never builds this node" is UNDETERMINED, and guessing either way would be a false finding |
| `AST_POSTFIX_EXPR` has no soundness coverage | NOT a hole for the common shape: in Go `i++` is a statement, and the expression-statement arm frees the taint WITHOUT marking, so under-marking it is unobservable. Only an operand with taint or side effects (`arr[f(p)]++`) makes it observable |
| A map READ marks its key escaping | Over-conservative, pinned deliberately as local row 22. `goo_map_get` stores nothing, so only the base needs marking. MEASURED reason to leave it: the daemon's string local escapes through a map WRITE, so tightening the read rule reclaims ~0% |
| `append`'s ELEMENTS still escape | Correct, not a gap: the buffer keeps a copy of each, so a pointer element genuinely outlives the call. It is why a `[]string`'s element strings are not reclaimed |
| Map ENTRY NODES are freed (#269); keys and values are NOT | `goo_map_dtor` walks the chain and frees each `GooMapEntrySV`. Keys and values stay: `goo_map_set_sv` stores both verbatim, so the map owns neither, and a key can be a literal's constant data. Who frees them is open, and it is the same question as slice elements |
| The daemon's `counts` is still REFUSED, so #269 buys it nothing | `counts[f] = counts[f] + 1` marks the map escaping on account of its own contents: `assign_to_lvalue` marks `rhs_taint`, and the right side carries the map's OWN taint through the `AST_INDEX_EXPR` arm. PR B is the SELF-STORE rule — subtract the lvalue base's self-bit before marking. Worth 822,000 of 2,209,982 per 2,000 requests |
| Named results are NOT in `LocalEnv` | Unchanged. Still the only sink covering `p = q; return` with a named result. Pinned by param_escape row 20 |
| Method summaries resolve by BARE name | Unchanged. `registry_find` returns the first match |
| `s += "x"` on a string is refused | Condition 4 counts it as a second binding -> RELEASE_NO_REBOUND. A release of the LAST value would in fact be correct, because every concatenation returns fresh memory. Relaxing condition 4 is a separate change with its own rows. Pinned by row 24 |
| A NAMED string or slice type is refused | `type Name string` has kind TYPE_NAMED, so codegen_arc_note_local's shape test fails. Lost reclamation, never an unsafe free |
| An integer `+` reads RELEASE_OK | Deliberate, and pinned by row 25. release_decision is pure AST with no type information, so it cannot tell a string `+` from an integer one. Costs nothing: the codegen layer refuses a bare i64 slot. Do NOT narrow the arm -- the two-layer split is what keeps each layer simple |
| Condition 5 is RETIRED (#267) | Kept here as the record: it refused every conditionally-declared local from #265 to #267. `codegen_arc_zero_slot` removed its premise, and the guarantee is FAIL-CLOSED — no release site is recorded unless the zero store was emitted |
| FIXED in #268: `escape_core`'s select arm | Kept as the record. It walked `sc->comm` -- an EXPRESSION -- with `escape_walk_stmt`, hitting a `default:` that calls `escape_mark_all`. Now routed through a shared `escape_walk_expr_stmt` that carries the channel-send sink. Row 32's tripwire fired and it now asserts RELEASE_NO_REBOUND |
| `assert`, `assume`, `asm`, label and `goto` stay unread | They keep RELEASE_NO_UNKNOWN reachable, which the teeth script needs. A label WRAPS a statement, so refusing on one is conservative for a real cause. Row 34 is the only row holding that verdict |
| Cycles | ADR 0002 names this as the real cost with no cheap fix |
| `interface == interface` fails codegen | Pre-existing, untouched |
| `types.c:603-607` frees interface methods as an ARRAY | Latent, pre-existing |
| `ownership_checker.c` compiles into `bin/goo` with ZERO call sites | Unchanged |

## Still true, and still unaddressed

- **There is no CI.** `gh pr view --json statusCheckRollup` returns `[]`. All six
  merges today rode on this one machine's local hooks, which
  `git push --no-verify` bypasses. This matters more now that the compiler frees
  memory: a wrong release is a use-after-free, not a lost optimisation. Standing
  open decision, because it costs money.
- Four conformance rows remain absent: `type_complex`,
  `expr_method_expression`, `generic_constraint_union`, `generic_type`.

## The direction, stated plainly

Last session's close-out said "nothing this session freed a single byte". This one
freed 115.8 MB on the benchmark that motivated ADR 0002, and the way it got there
was refusing to trust a projection. Five plans were invalidated by a measurement
that cost seconds, and four instruments were caught lying before their output was
believed.

`TYPE_STRING` shipped in #264, then #265 and #266 closed the switch cliff — and
between them they produced this session's sharpest lesson. The plan for #266 was
"add recursion plus rows". Reading the codegen first found a LIVE use-of-undef bug
that had been reachable through a plain `if` since #261, and that bug had to ship
FIRST because the four arms would have multiplied its reach. A plan that had gone
straight to the feature would have widened a memory-corruption path.

#267 then finished the job #266 started: the zero store converted condition 5's
refusal into reclamation, so a local declared inside an `if` or a `switch` case
body now reclaims like any other. Three increments, one live bug closed, and the
daemon moved by nothing at all — every one of them was worth shipping anyway,
because the bug was real and the precision is real, and the daemon simply does
not have the shapes they unlock.

THE MEASUREMENT DISCIPLINE PAID FOUR TIMES TODAY: a vacuous probe caught by
checking its debug line, a row that passed on the wrong condition, a comment
whose mechanism was wrong, and a plan that would have widened a corruption path
had it gone straight to the feature. None was found by review.

#268 fixed the select over-marking, and #269 gave the release path a destructor
so a map frees its entry chain. TWELVE PRs, and the daemon has not moved since
#264 — every one of them was still worth shipping, because two closed live bugs
and the rest bought precision the benchmark simply does not exercise.

THE MEASUREMENT DISCIPLINE PAID SIX TIMES TODAY: a vacuous probe, a row that
passed on the wrong condition, a comment whose mechanism was wrong, a plan that
would have widened a corruption path, and the SAME valgrind partial read twice.
None was found by review.

What changed today is that the next step finally has measured bytes behind it.
PR B is worth 822,000 of 2,209,982 per 2,000 requests, and #269 already built
the half that frees them. Everything after it is a real design, not an
increment.
